# Vue 知识库

Vue3 项目应重点掌握 Composition API、响应式系统、组件通信、生命周期和性能优化。简历中如果写了 Vue 项目，应能说明组件职责和状态设计。

## Composition API

setup、ref、computed、watch 适合组织页面状态和副作用。复杂页面可以将业务状态放在页面容器中，将展示逻辑拆分到子组件。

## 组件通信

父子组件通常通过 props 和 emits 通信。跨层级状态可以使用组合式函数或状态管理方案，但应避免为了简单页面过早引入复杂全局状态。
