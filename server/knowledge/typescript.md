# TypeScript 知识库

TypeScript 能提升前端工程稳定性。候选人应能说明接口类型、组件 props 类型、联合类型、泛型和类型收窄在项目中的实际价值。

## 类型建模

对于分析结果、评分卡、建议和面试题这类结构化数据，应先定义稳定的业务类型，再让页面组件基于类型渲染，避免 any 和隐式结构。

## 类型安全

接口返回数据需要校验和兜底。常见做法包括定义请求体类型、响应体类型、错误结构类型，并在使用前判断字段是否存在。
